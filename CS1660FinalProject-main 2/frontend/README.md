# Frontend (Danielle)

This folder contains the frontend code for Playing the Market.

Danielle:
- Build the UI here (HTML/JS or React/Vite).
- Your final build output should end up inside this folder.
- GitHub Actions will automatically upload everything in this folder to the S3 bucket.

Files to include later:
- index.html
- CSS files
- JS files
- React/Vite build output (if using a framework)
